# تسليم DUB- — مبني من الحالة المتحقق منها (2026-09-09)

النسخة السابقة من الملف ده (248 سطر) ضاعت مع الرنج بوكس لأنها **ما اتعملهاش commit**. النسخة دي
متعملها commit على فرع الجلسة، فمش هتتكرر. كل رقم هنا محسوب من الملفات في الشجرة أو من
`MEMORY.md` عند `6678ad5`.

فرع الجلسة: `arena/01a0880a-dub` (اتقدّم بـ`git merge --ff-only origin/arena/01a08487-dub` لحد
`6678ad5` عشان أدوات الجولة التالتة والـvoice shop تبقى في المتناول).

## 1. القواعد الثابتة (من المستخدم — لا تُناقش)

1. الصوت من أدوات الوكيل فقط: `add_voice` ثم `generate_speech`. Edge / Piper / XTTS / VoxCPM /
   eSpeak ليست مصادر صوت في أي جلسة.
2. **مفيش موسيقى.** المصدر مالوش موسيقى تُسترجع، و«الهواء» المقيس من هذا المصدر مرفوض
   (`source_room_tone()` لقى 9.8 dB بس تحت الكلام، و63% من «الوقفات» داخل نوى كلمات)
   → المسار المنشور `--air-source none`، ومفيش طبقة ضجيج مُلَفّقة تحت الصوت.
3. **مفيش حشو سكون** ومفيش تمديد: الوقفات الحقيقية للأصل ~0.161 ث، والفجوات تُقاس من العيّنات.
4. «كل كلمة يقابلها كلمة»: المقطع **يُعاد كتابته** مش يتمطط؛ العجز كلمة‑كلمة نافذة‑بنافذة.
5. اللهجة والإحساس بالأذن: الأرقام لا تسمع. البوابة `dubs/hajj-dream-2108415/voice-tests/dialect-stress.txt`
   (6 سطور، كل سطر فيه فخ) — أي صوت فيه خطأ مسموع في 1 من 6 **لا يُعتمد**.
6. ردود قصيرة، من غير أسئلة لو الحالة موثقة، ولا يُعرض أي لينك قبل تأكيد من API GitHub.
7. لا حذف لبيانات سابقة؛ النسخ القديمة تفضل بمرجع أرشيف متحقق.
8. الشغل على فرع الجلسة فقط، ولا دمج في `main` إلا بطلب صريح.

## 2. المسلَّم المنشور (متحقق في الشجرة)

| الملف | حجم | SHA-256 |
|---|---|---|
| `dubs/hajj-dream-2108415/source-sync/final-dub-source-sync.mp4` | 41,915,527 | `8b82081c153175e25fb9798727fcf11bc3cc403c5859d7986b5da37027d212f5` |

- تقرير البوابات: `dubs/hajj-dream-2108415/timing-repair/matched-render-verification.json`
  → `status: verified`، **10/10** بوابات صح (`video_stream_bit_identical`, `speech_untouched`,
  `master_chain_is_air_not_music`, `no_long_gaps`, `coverage_within_budget`,
  `clause_onsets_at_planned_time`, `no_quiet_hole_over_budget`, `clause_phase_matches_plan`,
  `loudness_in_range`, `durations_match`)، و`human_listening_certification: false`
  — أي: مفيش شهادة أذن بشرية، والتقرير ده فني فقط.
- النسخ الصوتية القديمة **مش في الشجرة** (اتأرشدت بالمرجع في `9048ea7`):
  `voice12/final-dub-voice12.mp4` — 11:16، كوميت `139e51f`، sha256 `510058ad4cc46a9dfc56279175796eadcb2eb818b26a171bee5d26fb64c6cb41`؛
  `voice07/final-dub-voice07.mp4` — كوميت `fce9243`، sha256 `248d448bdf5e30ec4d969f0fba974c73173a693df192e87f1cda9c114ef8cf1e`.
  تُستعاد بـ`scripts/materialize_lossless_takes.py` من مراجع FLAC الموثقة، **مش** بإعادة توليد.

## 3. ميزانية الجولة التالتة (`timing-repair/round3-brief.md` + `.json`)

123 مقطعًا فوق 630.6 ث. **مسموح 1633 كلمة / منشور 1457** → عجز **176 كلمة (≈11%)** — مش 300+،
و«أرقّ بنصف النص» قراءة غلط قديمة (ربط clauses كان مختل: 641 كلمة لـ123 مقطعًا).

| نافذة | ثوانٍ | مسموح | منشور | فرق |
|---|---|---|---|---|
| p0 (0.0–63.5) | 63.5 | 157 | 147 | +10 |
| p1 (63.5–148.7) | 85.2 | 217 | 182 | +35 |
| p2 (148.7–248.4) | 99.7 | 257 | 207 | +50 |
| p3 (248.4–333.5) | 85.1 | 215 | 184 | +31 |
| p4 (333.5–411.8) | 78.3 | 195 | 163 | +32 |
| p5 (411.8–504.3) | 92.5 | 214 | 201 | +13 |
| p6 (504.3–593.8) | 89.5 | 201 | 200 | +1 |
| p7 (593.8–676.33) | 82.5 | 177 | 173 | +4 |

الطباع المطلوبة (مش كلها محايد): `neutral 73، excited 16، tense 12، warm 12، grief 4، solemn 3،
playful 3` — ولكل مقطع `pace_target` و`punch` و`sentence_shape` في `round3-brief.md`.

## 4. سرعة كل صوت = ثمن النص

الهدف **2.507 كلمة/ث** (1585 كلمة فوق 632.5 ث كلام أصل، وقفات الأصل 19.99 ث). اللي يبلع النص كامل:

| صوت | ك/ث | سعة 632.5 ث | الحكم |
|---|---|---|---|
| voice-07 | 2.545 | 1609 | النص كامل يدخل، والفاضل تنفّس طبيعي |
| voice-11 | 2.516 | 1591 | النص كامل يدخل |
| voice-13 | 2.509 | 1586 | النص كامل يدخل |
| voice-12 | 2.353 | 1488 | لازم يُقطع 97 كلمة (6.1%) — لا تمديد |
| voice-08 | 2.336 | 1477 | −107 |
| voice-05 | 2.331 | 1474 | −111 |
| voice-06 | 2.255 | 1426 | −159 |
| voice-10 | 2.225 | 1407 | −178 |
| voice-04 | 2.222 | 1405 | −180 |
| voice-09 | 2.172 | 1373 | −211 |

الملفات: `voice-tests/gallery/pace-fit.json` و`candidate-ranking.json` و`all-10-voices-in-a-row.mp3`.

## 5. صفحة المئة عينة

- `docs/voice-shop.json`: **100 خانة — 85 `ready` (صوت حقيقي) — 15 `queued`**، مبنيّة على
  **21 ملف `source_take`** خام × 4 أعمار × 4 نبرات، بـ`scripts/build_voice_catalog.py`.
- منشورة ومختبَرة من الرابط نفسه: https://zaldynbwtany202-dev.github.io/DUB-/voice-shop.html
  (PR #2 → `main` = `7d7827d`، أبوه التاني `3a238e5`، و`Deploy GitHub Pages` run 34402304920 = success).
- كل صف فيه `chain` + `chain_check` (نغمة 200 هرتز: +4.00 / −3.51 نصف نغمة، مدة 0.996–1.0).
  القياس على الكلام نفسه بيكذب (قال +2.4 بدل +4).
- **`docs/voice-choice.json` مش موجود في أي فرع** → الاختيار من الصفحة ما وصلش للمستودع،
  والجولة التالتة مشروطة بيه. `scripts/set_voice_choice.py` بيرجّع الرقم للكتالوج وبيرفض
  أي `chain` بعتّها المتصفح.

## 6. حدود المنصة المقيسة

- اللهجات: كل الأصوات المسجلة **رفضت** `ar-SA / ar-MA / ar-AE / ar-DZ / ar-KW / ar-LB / ar-IQ /
  ar-TN / ar-JO`، وقبول `ar` و`ar-EG` بس (اتجرّب فعلاً). مفيش مقطع مصري اتلبس فلتر واتقال عليه سعودي.
- `generate_speech`: **10 مقاطع في اللفة**؛ ودفعة الترشيح = 5 أزواج (10 أصوات) في نفس الرد.
- **`voice_id` بيموت مع نهاية الجلسة** — اللي بينجا هو sha256 + رابط FLAC المؤرشف في git.
- مرفوضة من رمز Arena (ما تعاودش تجربتها): إضافة/تعديل ملف workflow (`refusing to allow a GitHub
  App to create or update workflow`), `workflow_dispatch` = 403, `PUT /repos/.../pages` و
  `POST /pages/builds` = 403. بايج الوحيد: ملفات `docs/` على `main`.

## 7. آخر حالتين — رفض متراكم، ومفيش صوت جديد لسه

- الجلسة دي طلّبت التلات أصوات اللي ضاعت بـ`add_voice` index 0–2 (`voice-00/01/02`) → **«كلها سئ»**.
- بعدها دفعة index 3–7 (`voice-03..voice-07`) → **«كلها سئ»** برضه. **10 من 10 مرفوضين.**
- **مفيش ولا مقطع اّتولّد بأي صوت مرفوض**، ومفيش تعديل في `voice-shop.json`، ومفيش حاجة اسمها
  `voice-choice.json`. التسع مقاطع الخام وتصحيحات اللهجة الخمسة اللي في الرسالة **لسه متعملتش**
  (محتاجة صوت مقبول الأول) — مش مفقودة، دي مستنيا.
- أي دفعة تانية تبدأ من **index 8**.

## 8. الأفخاخ التقنية اللي اتدفعت فيها وقت

1. `vocal-pauses.json` فخ: `spans` = وحدات **كلام** الأصل (125، متوسط 5.248 ث). الوقفات الحقيقية
   في `pause_list` (124، متوسط 0.161 ث، الإجمالي 19.99 ث). قراءة المفتاح الغلط بتحط صوت الراوي
   الأصلي نفسه تحت الدبلجة عند −25.9 dBFS.
2. `pause_spans()` يختار بالشكل (متوسط < 1 ث) وبيقاطع نوى كلمات `words.json` — لا تتحايل عليه.
3. مواضع الجُمل ما تُقِرش بـVAD على المكساج (السرير بيطمس البدايات: 38 صفحة «بلا بداية» كاذبة)؛
   قِس على الماستر الجاف، والفجوات من العيّنات (ذروة < −55 dBFS على 10 مللي‑ثانية).
4. `level_sd_db` على الكلام فوق `floor+6 dB` بس؛ الوقفات بتخلي أي عينة تبان «أكثر حيوية» كذبًا.
5. `np.correlate` على 30 ث = 20 دقيقة فحص → FFT على 4 ث (كل الاختبارات 0.3 ث).
6. `chain_air_evidence` لازم يرجّع `bool` حقيقي وقيم Python scalar، وإلا JSON المنشور بيقع في
   `np.bool_`/NaN.
7. أي رقم في `MEMORY.md` بعد كوميتات الأرشيف ممكن يكون **مرجع** مش ملف في الشجرة — اتأكد
   بـ`git ls-tree` قبل ما تعرض لينك أو تقول «الملف موجود».
8. الرنج بوكس الجديد مالوش `ffmpeg`/`numpy`/`soundfile`:
   `python3 -m venv .venv && .venv/bin/pip install numpy soundfile imageio-ffmpeg`
   (`.venv/` مستثناة في `.gitignore:9`)؛ النسخة المرفقة فيها `libmp3lame` و`flac`.
9. الكlon **shallow** (`git merge-base` بين `main` وفرع الجلسة بيفشل) — `--is-ancestor`
   مش موثوق هنا؛ استخدم أبوة كوميت الدمج نفسه.

## 9. التنفيذ بالترتيب

1. `git fetch origin && git merge --ff-only origin/arena/01a08487-dub` (اتعمل في الجلسة دي).
2. تهيئة الأدوات (البند 8.8)، و`git ls-remote origin` للتأكد إن فرع الجلسة على GitHub.
3. **قرار المستخدم الأول: رقم صوت.** لا توليد قبله (البند 1.5). الأربعة المحتملة:
   (أ) رقم من `voice-shop.html`، (ب) دفعة ترشيح جديدة من index 8، (ج) إعادة صوت منشور
   مؤرشف — **غير ممكن** لأن الـid مات مع الجلسة، (د) تجميد الجولة التالتة.
4. لو اتقال رقم: `add_voice` → `generate_speech` بـ**9 مقاطع FLAC** (c1 = سطور 1+2،
   c2 = 3+4، c3 = 5+6)، تحويل MP3 محلي بدون إعادة ترميز ناقص، تحديث `voice-tests/dialect-check.json`
   (سطور + ثوانٍ + sha256 للـFLAC والـMP3)، ثم `build_voice_catalog.py --extra '<new flacs>'`
   و`set_voice_choice.py`، و`verify_source_sync_render.py` + مجموعة الاختبارات المستهدفة.
5. اكتب 176 الكلمة الناقصة نافذة‑بنافذة حسب `round3-brief.md` (بلا تمديد، بلا حشو سكون)،
   وسجّل تصحيحات اللهجة (قبل/بعد لكل سطر مريض بالفصحى) في `timing-repair/` كملف reviewable.
6. `git add -A && git commit && git push origin arena/01a0880a-dub` ثم تأكيد
   `gh api repos/zaldynbwtany202-dev/DUB-/git/ref/heads/arena/01a0880a-dub` **قبل** أي لينك.
7. لو اتعمل `pending-pushes/` لأي سبب: امسحه أول ما الـpush يتأكد، وحدّث `MEMORY.md` في نفس اللفة،
   و**اعمل commit للملفات اللي المفروض تنجا** — دي القصة اللي الملف ده اتكتب عششانها.
