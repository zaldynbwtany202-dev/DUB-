# الموديلات المستخدمة في المنظومة · Model Stack

كل موديل في خط الدبلجة، دوره، وكيف يُحمَّل. **لا تُرفع أوزان الموديلات إلى المستودع** (أحجامها بالجيجابايت ورخصها تمنع ذلك) — كلها تُنزَّل تلقائيًا عند أول تشغيل أو تُستدعى سحابيًا.

## خط المعالجة الكامل

| # | المرحلة | الموديل | الحجم | التحميل |
|---|---|---|---|---|
| 1 | التفريغ الصوتي بتوقيتات الكلمات | **faster-whisper** (`small` افتراضيًا، حتى `large-v3`) | ~0.5–3 GB | تلقائي عند أول `dub run` |
| 2 | فصل الحوار عن الخلفية | **demucs `htdemucs`** | ~80 MB | تلقائي عند أول تشغيل |
| 3 | الترجمة | **Google Translate / MyMemory** (عبر deep-translator، مع تراجع تلقائي) | — | خدمة مجانية بلا مفتاح |
| 4 | TTS محلي — سريع بأصوات جاهزة | **Piper** (Apache 2.0، عربي + 30 لغة) | ~30–80 MB لكل صوت | تلقائي |
| 5 | TTS محلي — استنساخ تجاري | **Chatterbox** (Resemble AI، رخصة MIT) | ~1.5 GB | تلقائي |
| 6 | TTS محلي — استنساخ SOTA | **F5-TTS** (CC-BY-NC، غير تجاري) | ~1.2 GB | تلقائي |
| 7 | TTS محلي — Coqui | **XTTS v2** — 17 لغة منها العربية | ~2 GB | تلقائي (يتطلب `COQUI_TOS_AGREED=1`) |
| 8 | TTS سحابي | **Minimax** `fal-ai/minimax/voice-clone` + `speech-2.8-hd` | — | `FAL_KEY` |
| 9 | TTS سحابي | **ElevenLabs** Instant Voice Clone + Multilingual v2/v3 | — | `ELEVENLABS_API_KEY` |
| 10 | الدمج والمعايرة | **ffmpeg** (atempo / amix / loudnorm −16 LUFS + crossfade) | — | حزمة نظام |

## مقارنة سريعة بين محركات الاستنساخ المجانية

| المحرك | الرخصة | استنساخ | جودة عربية | استخدام تجاري |
|---|---|---|---|---|
| **Piper** | Apache 2.0 | ❌ (أصوات جاهزة) | ممتاز | ✅ |
| **Chatterbox** | MIT | ✅ من ~6ث | جيد | ✅ |
| **F5-TTS** | CC-BY-NC | ✅ SOTA من ~10ث | الأفضل بين المحلية | ❌ |
| **XTTS v2** | CPML | ✅ من ~10ث | جيد جدًا | ❌ |

## ملاحظات تشغيلية مهمة

- **الاستنساخ يحدث أثناء التشغيل**: الأداة تقصّ عينة نظيفة لكل متحدث من مسار الحوار المفصول (~10 ثوانٍ تكفي) وتستنسخها لحظيًا — لا حاجة لأي ملفات موديلات مسبقة.
- **معرّفات الأصوات المستنسخة غير قابلة للنقل بين الحسابات** السحابية — كل تشغيل يستنسخ من جديد (سلوك مقصود لأسباب قانونية وخصوصية).
- **الصوت الأصلي للمتحدث لا يُرفع للمستودع** — العينات تُبنى محليًا داخل `dub_work/` وهي مستثناة في `.gitignore`.
- XTTS على CPU يعمل لكنه أبطأ (~10–30 ثانية للجملة)؛ GPU يختصرها لثوانٍ.
- Google Translate قد يحجب عناوين مراكز البيانات — لهذا يوجد التراجع التلقائي إلى MyMemory، أو استخدم `--translate-backend file` بترجمة بشرية.

## مثال إعادة إنتاج كامل

شغّل الدبلجة الاحترافية نفسها التي أُنتجت بها عينة WWE:

```bash
dub run examples/wwe/source.mp4 --src en --tgt ar \
    --engine minimax \
    --translate-backend file \
    --translations-file examples/wwe/translations_ar.json \
    --speaker-map "0:S1,1:S2,2:S1,3:S2,4:S1,5:S2,6:S1,7:S2" \
    --out dubbed_ar.mp4
```

(التفاصيل في `examples/wwe/run.sh`.)
