# ذاكرة دائمة — لا تُنسى

هذا عقد حي. يُقرأ قبل أي عمل. لا يُختصر حتى يضيع.

## الصوت

- **الصوت الدائم الوحيد: `voice-33`.**
- المستخدم قال: جيد. احفظ هذا الصوت. سنستخدمه دائما. ثم: أنا أتحدث عن الصوت، احفظه في الذاكرة أيضاً.
- الملفات: `library/voices/locked-narrator/` (`voice-33-bank.mp3` + `seg-0000`…`seg-0003`).
- القفل: `dubs/0911-1/LOCKED_VOICE.json`
- أي `generate_speech` بعد الآن: `voice_id=voice-33` فقط.
- **قيد واقعي مسجَّل 2026-09-14:** `voice_id` مرتبط بالجلسة التي اختير فيها؛ في جلسة `arena/01a09fa1-dub` ترجع الأداة «Unknown voice_id voice-33». إذن تسجيل أي كلام جديد يستلزم `add_voice` واختيار المستخدم، مع مقارنة العينة المحفوظة `library/voices/locked-narrator/voice-33-bank.mp3`. المستخدم فوّض الاختيار بقوله «اختر الأنسب، المهم النظام يشتغل». التسجيلات العشر المقفولة تبقى كما هي ولا تُمس.
- **القرار الناتج (2026-09-14):** اختار المستخدم `voice-00` من المعاينة (نص الافتتاحية الحقيقي، `ar-EG`، رجالي، سرد). قيس فعلياً: **11.64 حرف/ث** مقابل 11.58 لـ`voice-33` — أي نفس الإيقاع عملياً، فالخطة والسرعات صالحة بلا تعديل. سرد «حكاية داس» الكامل يكتمل بـ`voice-00`.
- **لا خلط صوتين في فيديو واحد.** المجموعات 0–9 المسجَّلة بـ`voice-33` تُعاد بـ`voice-00` قبل أي نشر نهائي. المقطع الحالي بصوت واحد: `dubs/das-full/matched/final-dub-das-voice00-10-19.mp4` (‏231 ث = الأصل من 237.95 إلى 468.5 ث).
- ميكس 0911-1 المعتمد (`v33/final-dub-v33.mp4`) يبقى بـ`voice-33` ولا يُمس.
- ممنوع: `add_voice` لراوي 0911-1، ممنوع voice-18/19/28/29/31/32، ممنوع تحويل طبقة/formant/clone DSP على هذا الصوت.
- بعد التسجيل: لصق في الفيديو فقط.

### قرار 2026-09-17 — صوت دبلجة الأفلام مقفل

- المستخدم بعد سماع الفيلم الكامل (doctor-lecture، 19:20، 49 مجموعة): **«جيد جدا اريد هذا الصوت دائما للدبلجة احفظه هل فهمت اريده دائما للدبلجة تبع الافلام»**.
- **صوت دبلجة الأفلام الدائم = `library/voices/film-dub-narrator/`** (البنك `voice-film-bank.mp3` دقيقتان + 8 عيّنات + `LOCKED_VOICE.json`). سُجّل في تلك الجلسة باسم `voice-01`، والمعرّف مرتبط بالجلسة فلا يُعتمد؛ **المرجع هو الملفات الصوتية لا الاسم**.
- الإيقاع المقاس لهذا الصوت: **9.21 حرف/ث** (المدى 8.19–10.89) من 49 تسجيلًا قبل أي `atempo`. أي تخطيط جديد يبدأ من هذا الرقم لا من 8.69.
- وسطي F0 المقاس 206 هرتز (190–216) — دلالة مساندة فقط، و`gender` في القفل `null` عن قصد: الصوت الرجالي الموثّق `voice-00` يقيس 154–198 هرتز أي داخل المدى نفسه، فالطبقة لا تُعرّف الصوت ولا تحسم نوعه.
- **لا بوابة آلية للتحقق من هوية الصوت، وهذا مقاس لا مُفترض:** `scripts/voice_metric_study.py` قاس سبع ميزات طيفية على 1176 زوجًا من الصوت نفسه و1568 زوجًا من صوت مختلف؛ أفضل هامش 0.55× وخطأ متساوٍ 5.1٪ (التوزيعان متداخلان). بوابة بأقرب جار عايرتُها على 8 عيّنات بدت نظيفة (20.1 مقابل 33.2) ثم أخطأت في 3 من 7 اختبارات خارج المعايرة، فأُلغيت. `scripts/verify_voice.py` أداة قياس ومقارنة سمعية (`--ab`)، **والحكم للمستخدم وحده**.
- ممنوع: استبدال هذا الصوت في أي دبلجة أفلام، خلط صوتين في فيديو واحد، `pitch`/`formant`/`clone` DSP على تسجيلاته. المعالجة الوحيدة `atempo` ثم دمج.
- **`voice-33` يبقى لمشروع 0911-1 فقط** وميكسه المعتمد `v33/final-dub-v33.mp4` لا يُمس. عيّناته في `library/voices/locked-narrator/` غير متجانسة قياسياً (34–195 بينها) فلا تُستخدم مرجعاً معايراً.
- **`voice-00` لم يعد صوت الأفلام.** تسجيلات das-full به (المجموعات 0–23 و25 و26 و29 و30، أي 28 تسجيلًا) لا تُخلط مع الصوت الجديد.
- **توضيح المستخدم 2026-09-17:** «نعم لا يجوز اخبرتك فقط بان هذا الصوت يعجبني» — أي أن حظر الخلط مؤكَّد، وأن رسالته كانت **تفضيلًا للصوت لا أمر عمل**. فالعمل التنفيذي على das-full **مؤجَّل** حتى يطلبه: وعندها يُسجَّل كله بالصوت المقفل وتُعاد الـ28 مجموعة، ولا يُسلَّم فيديو واحد بصوتين. لا يُسأل المستخدم عن هذا ثانيةً إلا إذا طلب استكمال das-full.
- **وصفة استرجاع الصوت مُختبَرة عمليًا (2026-09-17):** لأن `voice_id` لا يعبر الجلسات، فالمعاينة هي الطريق. `add_voice` بـ`language=ar-EG` و`voice_identity.gender=masculine` و`use_case=narration` ونصّ الافتتاحية المحفوظ في `LOCKED_VOICE.json:retrieval` ⇒ المرشَّح الذي قبله المستخدم بأذنه (`voice-04` في تلك الجلسة) قيس على مسافة **19.39** من البنك، أي داخل مدى الصوت نفسه (≤20.07) وأبعد من أي صوت مختلف معروف (أدنى ما بلغه `voice-00` تاريخيًا 33.16، وهنا 56.73).
- **الإجراء في كل جلسة جديدة، بالترتيب:** ① استماع إلى `voice-film-bank.mp3` قبل التصويت ② معاينة بالمعاملات أعلاه ③ مسبار بنصّ المجموعة 0 (388 حرفًا، مطابق لـ`seg-0000`) يُحفظ في `checks/` ④ `scripts/verify_voice.py --probe` ⑤ `--ab` والحكم **لأذن المستخدم وحده** ⑥ **ممنوع بدء أي تسجيل دبلجة قبل قبول المستخدم صراحةً**.
- **الإرشاد السابق «يُترك `gender` بلا تحديد» مُبطَل بالقياس:** تركه بلا تحديد أعطى صوتًا أدني بأوكتاف (115.1 مقابل 206.4 هرتز، مرفوض)، وتحديده أنثويًا أعطى مسافة 169.8 (مرفوض رفضًا قاطعًا). الصواب `masculine`. المسبارات الثلاثة محفوظة في `library/voices/film-dub-narrator/checks/` مع أسبابها.
- **الطبقة ليست دليل تطابق:** المرشَّح المقبول يقيس F0 أدني من مدى البنك، والفارق 18–40 هرتزًا بحسب المُقدِّر (نافذة 32ms مقابل 25ms). الاتّجاه ثابت والمقدار لا، فالأذن هي التي حسمت. لا يُدَّعى تطابق مقيس في أي وثيقة.

## الفرع

- العمل على `arena/01a0922c-dub` فقط. **ممنوع الدمج.**

## النص

- كليب 0911-1: الكلمات من `LOCKED_TRANSCRIPT.json` فقط. Whisper ليس مصدر الكلمات.
- الفيديو الكامل (حكاية داس، 52:22): نفس الصوت `voice-33`. المصدر `library/das-full/source.mp4`.
- **ممنوع تسجيل Whisper.** التفريغ الآلي توقيت فقط. الكلمات من سكربت المستخدم. `dubs/das-full/part1/final-dub-part1.mp4` مرفوض: كلشي خاطئ.
- سكربت الكامل من يوتيوب `NB2YcTh_L6k` (نفس المدة 52:22، قناة ملخص انمى كامل): `library/das-full/youtube/unique.txt`. ليس Whisper.
- `NB2YcTh_L6k.words.srt` مرفوض: توزيع داخل نافذة 30 ثانية ليس وقت النطق.
- كلمة بكلمة مع وقت النطق: `NB2YcTh_L6k.spoken.srt` — الكلمات من يوتيوب، الأوقات من الصوت (`full.wav` / whisper.cpp توقيت فقط).

## تصحيح النص المقفول (2026-09-16)

- الكلمات تبقى من `library/das-full/youtube/unique.txt`، لكن ترجمة يوتيوب التلقائية كتبت الشخصية الواحدة بأشكال مختلفة («داس»/«دس»/«دايس»/«داساني»، «الانا»/«الانه»، «مول»/«مور») فالصوت كان يقرأ اسمين مختلفين للبطل. المستخدم أبلغ عن هذا كعيب ⇒ التوحيد مقبول ومطلوب.
- الأداة الوحيدة المسموح بها: `scripts/apply_script_corrections.py` (استبدال كلمة كاملة، مع لوازم متصلة و/ف/ب/ل/ك). الناتج `groups-v3.json` + `script-corrections-diff.md` للمراجعة.
- **التوقيتات لا تُمسّ إطلاقاً**: النوافذ منسوخة حرفياً من `groups-v2.json`، فلا انحراف في المزامنة، والتسجيلات التي لم تتغير كلماتها تبقى صالحة.
- لا تُستبدل كلمة يشتبه في معناها (القائمة في `script-corrections-diff.md`): التخمين أسوأ من الخطأ الظاهر.
- «انا» ضمير متكلم وليست اسماً — لا تُمسّ. كذلك «الامور» و«الاسطوري» و«دسوا».
- الالتزام الفوري إلزامي: كل تسجيل يُلتزم في نفس الجولة. الجولة التي تُقطع تفقد ملفاتها غير الملتزمة (حدث فعلاً: ضاعت 24 و27 و28 و31).

## استقبال فيديو جديد (2026-09-18)

- **المستخدم:** «بمجرد ان يرفع اليك الفيديو تنقله مباشرة الى المستودع في مجلد خاص». قاعدة دائمة: **لا صندوق مؤقت**. `inbox/` كان يضيع مع كل انقطاع للـsandbox.
- الوجهة `library/<slug>/` لكل فيديو مجلد خاص. خادم المعاينة `.preview/serve.py` **يلتزم ويدفع تلقائيًا** فور اكتمال الملف، في خيط خلفي حتى لا ينتظر الردّ على الدفع، ولا يُمرَّر إلى `git add` إلا مسارات الاستقبال نفسها — فلا يجرف `git add -A` في مكان آخر مصدرًا وزنه 150 م.ب.
- `library/<slug>/INTAKE.json` = الحقائق الثابتة: الحجم، `sha256`، عدد المقاطع، التخطيط، و`fed_to_pipeline` (ما يُغذَّى للخط). حالة الدفع المتغيّرة في `.cache/intake-status/<slug>.json` لأن الملف الملتزَم يجب أن يبقى ثابتًا وإلا اتّسخت الشجرة بعد كل رفع (عيب كشفه الاختبار وأُصلح).
- مصدر أكبر من **95 م.ب**: أجزاء في `library/<slug>/parts/` (حدّ GitHub 100 م.ب للملف الواحد)، والنسخة المجمَّعة `*.local.*` متجاهَلة في git ويُعيد بناءها `scripts/join_parts.py`. ما دون ذلك ملف واحد.
- الرفع **مقاطع 8 م.ب على 6 اتصالات متوازية مع استئناف** عبر `/upload-status`. السبب مقاس: الخادم يبثّ 200–434 م.ب/ث محليًا فليس عنق الزجاجة، بل قناة المعاينة تحدّ الاتصال الواحد.
- مُختبَر: 30 م.ب في 4 مقاطع مع استئناف متعمَّد، و144 م.ب في 19 مقطعًا → أجزاء×2 أكبرها 95.0 م.ب، وكلاهما أعاد `sha256` مطابقًا للأصل (وبصمة das-full طابقت القيمة المثبّتة `c93bfe5f…`). الدفع التلقائي مُختبَر مرتين (`c250f3e` و`570b2fa`) والشجرة نظيفة بعده.
- **بعد الوصول لا يبدأ التسجيل قبل:** ① التحقق من البصمة والمدة والدقة ② تحديد **مصدر الكلمات** (رابط يوتيوب أو نصّ المستخدم — التفريغ الآلي للتوقيت فقط، وهذه قاعدة سابقة) ③ استرجاع الصوت المقفل بوصفة `library/voices/film-dub-narrator/LOCKED_VOICE.json:retrieval` وقبول المستخدم **سمعًا** قبل أي تسجيل.

## الموسيقى الخلفية في الدبلجة (2026-09-18)

سُئل المستخدم عن فيديو السندباد: هل نُبقي الموسيقى الخلفية الأصلية تحت صوت الدبلجة؟ أجاب «نعم احتفظ بها». فالقاعدة: الدبلجة تُبنى فوق الموسيقى الأصلية لا محلها.

كيف تُنفَّذ: `scripts/separate_background_segmented.py` يفصل الموسيقى بنموذج UVR (أربع نطاقات، ONNX) عن صوت الراوي، ثم تُمزج الموسيقى تحت الدبلجة. النقاط المهمة المُقاسة:

- الفصل يعمل بنحو 1.1× الزمن الحقيقي، لكنه **يجب أن يُجزَّأ**: ملف 37 دقيقة يحتاج ~3.6 جيجابايت من الذاكرة للطيف وحده مقابل 2.67 متاحة. بمقاطع 110ث ينزل الاستهلاك ويبقى المعدّل 1.09×؛ بمقاطع 280ث اختنقت الذاكرة فصار 6× أبطأ.
- المزيج: `amix` يُخفض الجهارة بقسمة عدد المدخلات، فيُقسَّم على 5 عند تجميع المقاطع المتتابعة خطأً. الواجب `normalize=0`.
- المستوى المستهدف: الموسيقى ~13 ديسيبل تحت الصوت. بذلك يطابق المزيج مستوى الأصل (−22.5 مقابل −22.2 ديسيبل).

الفصل تقديري لا مثالي (`is_exact_original_stem: false`) — فهو قناع عصبي على طَور المصدر، لا مسار الاستوديو الأصلي.

## الصندوق قابل للمسح (2026-09-19)

وقع ذلك فعلًا: أُعيد بناء الصندوق من الصفر، فعاد الفرع إلى أصله `11edd5a` وضاع كل ما فيه. ما نجا وما ضاع:

**نجا** — كل ما هو ملتزَم في git. التسجيلات بالذات نجت لأن `!work/*/takes/*.mp3` أُضيف إلى `.gitignore` قبلها؛ لولا ذلك لضاعت ثمرة أبطأ خطوة في العمل (10 مقاطع لكل دور).

**ضاع** — `.venv` و`.cache`، لأنهما مستثنيان من اللقطات. ضاع معهما فصل الموسيقى (40 دقيقة حساب) والبيئة كلها.

**ضاع ثم استُعيد** — الفيديو المصدري، لأن `.gitignore` يستثني `library/*/*.local.*`. لكن أجزاء الرفع في `parts/` كانت ملتزَمة، فأُعيد تجميعه:

    cat library/<slug>/parts/*.mp4.part-000 library/<slug>/parts/*.mp4.part-001 \
        > "library/<slug>/<name>.local.mp4"

ثم تُقارَن البصمة بـ `sha256` في `INTAKE.json` لتأكيد التطابق بايت ببايت. **الأجزاء في `parts/` هي نسخة النجاة من الفيديو — لا تُحذف.**

إعادة بناء البيئة بعد المسح:

    python3 -m venv .venv
    .venv/bin/pip install -r requirements-background.txt

يكفي ذلك للدبلجة كلها (ffmpeg + فصل الموسيقى)، ولا حاجة لتثبيت `requirements.txt` كاملًا وما فيه من torch.

الدرس: أي منتج لا يُلزَم في git فهو هالك. وما يحتاج حسابًا طويلًا يُكتب له سكربت يُستأنف، لا أن يُنجز يدويًا في جلسة واحدة.
