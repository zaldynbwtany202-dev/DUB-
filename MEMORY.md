## طلب إصلاح المزامنة — لا تعتبر النسخة السابقة مرضية

- المستخدم اعترض على الفجوات وطلب أن يغطي كل كلام الأصل بمعناه وترتيبه وتوقيته، مع تحسين الصياغة إذا لم تناسب المدة. الصوت يبقى voice-12؛ لا إعادة اختيار أصوات.
- قياس المقارنة بالأصل كشف 80.32 ثانية من كلام المصدر لا تغطيها الدبلجة، وأطول فجوة 5.69 ثانية. القياس القديم الذي قارن الصوت قبل وبعد المكساج لا يكفي.
- اكتمل تفريغ جديد للأصل: 1855 كلمة ASR مع نقاط DTW في `timing-repair/source-analysis/words.json`. النص آلي ويُراجَع، ولا يمثل شهادة خلو من الأخطاء.
- حالة العمل `repairing_source_synchronous_narration`. لم تُنتج نسخة مصححة كاملة بعد. النسخة السابقة محفوظة في `voice12/timing-v1-archive.json` بمرجع GitHub متحقق منه؛ لا تفقد بياناتها.
- توجد مسودة إصلاح الافتتاحية حتى 63.5 ثانية في `timing-repair/opening-repair-script.json`، مرتبطة بكل معرفات كلمات المصدر في النافذة، مع تصحيح الصندوق إلى كبير وإعادة التفاصيل المحذوفة.

## الحالة الحالية — نسخة voice-12 مكتملة ومنشورة

- وافق المستخدم على العينة الطويلة voice-12 بقوله «نعم جيد». اكتملت دبلجة فيديو الحج كله بهذا الصوت، **وليس voice-07** الذي طلب استبداله.
- **الرابط المتحقق منه:** https://raw.githubusercontent.com/zaldynbwtany202-dev/DUB-/arena/01a07c92-dub/dubs/hajj-dream-2108415/voice12/final-dub-voice12.mp4
- الملف `dubs/hajj-dream-2108415/voice12/final-dub-voice12.mp4`، حوالي 11:16. كوميت الفيديو `139e51f`، SHA-256 `510058ad4cc46a9dfc56279175796eadcb2eb818b26a171bee5d26fb64c6cb41`؛ طابقت بيانات GitHub حجم الملف وبصمة كائن Git بعد الرفع.
- أُعيد استخدام تسجيل العينة المعتمدة ذي الـ56 ثانية نفسه للمقطع الأول. أُنتجت 8 تسجيلات أخرى من أداة الوكيل بنفس voice-12، وأُرشفت بدون فقد في Git بعد التحقق. كل المراجع في `voice12/script.json`.
- 9 تسجيلات، 33 موضع سرد، لا إبطاء، وأقصى تسريع نحو 7.8%. ملف صوت غير مضغوط 48 kHz للمزج، وخلفية أصلية مستخرجة آلياً وهادئة تحت السرد.
- صورة المصدر كاملة: 20288 حزمة مطابقة؛ جهارة −16.11 LUFS وذروة −2.87 dBTP، دون أخطاء فك ترميز أو اقتطاع كلام. نجح 66 اختباراً مستهدفاً. لا تدّع أن ذلك يثبت جاذبية الأداء أو صحة اللهجة بشرياً؛ المستخدم وافق على العينة فقط.
- نسخة voice-07 السابقة محفوظة في أرشيف ثابت متحقق منه، ورابطها في `voice07/final-archive.json`؛ لم تُحذف بياناتها من تاريخ Git. لا تعرض رابط الفرع القديم لها وكأن الملف ما زال في شجرة العمل.
- آخر حالة `complete` و`published_verified`. لا تطلب بدء التسجيل من جديد أو إعادة اختيار الصوت اعتماداً على حالات الانتظار الأقدم أدناه.
- جميع العمل على `arena/01a07c92-dub` فقط. الكاش الكبير مستثنى من الحفظ، لذلك تُستعاد التسجيلات من مراجع FLAC الموثّقة بدلاً من إعادة التوليد.

## أحدث ملاحظة جودة — اختيار راوٍ بديل، وليس إعادة تسجيل الفيديو كله

- بعد نشر voice-07، قال المستخدم إن الصوت غير جذاب واللهجة والنطق سيئان. يريد تحديداً **narration مصري رجالي احترافي**. لا تعتبر اعتماده القديم لعينة voice-07 موافقةً على النتيجة الأخيرة.
- أُوقف اعتماد النسخة الجديدة وأُبقي الفيديو المنشور والمواد القديمة دون حذف. طلب التصنيف narration لا يضمن وحده جودة اللهجة.
- عُرض زوج جديد index 12 (ar-EG، masculine، narration)، واختار المستخدم `voice-12` من الزوج. هذا ترشيح لا إذن بتوليد 11 دقيقة.
- أُنتج اختبار أطول من افتتاحية النص الحقيقي: 56.30 ثانية، بدون تعديل سرعة أو صوت أو خلفية. مرجعه الدائم `dubs/hajj-dream-2108415/voice-tests/voice-12-narration.json`، وفيه رابط أرشيف FLAC متحقق من مطابقته عينياً لخرج WAV الأصلي.
- **انتظر موافقة المستخدم على المقطع الطويل قبل أي تسجيل جماعي أو استبدال الفيديو.** الحالة `awaiting_replacement_narrator_review`، والمرشح voice-12. لا تغيّر هوية تسجيلات voice-07 القديمة في بياناتها.

## الحالة الحالية المعتمدة — دبلجة الحج voice-07 منشورة ومتحقق منها

- **اكتملت النسخة التي طلبها المستخدم بالصوت المصري المختار voice-07 وتحسين الصياغة والوقفات.** الملف: `dubs/hajj-dream-2108415/voice07/final-dub-voice07.mp4`، حوالي 11:16.
- **رابط GitHub متحقق فعلياً الآن**: https://raw.githubusercontent.com/zaldynbwtany202-dev/DUB-/arena/01a07c92-dub/dubs/hajj-dream-2108415/voice07/final-dub-voice07.mp4
- كوميت الفيديو `fce9243`، SHA-256 `248d448bdf5e30ec4d969f0fba974c73173a693df192e87f1cda9c114ef8cf1e`. طابق حجم الملف وبصمة كائن Git نتيجة API بعد الرفع. لا تطلب إعادة الربط أو إعادة الدبلجة اعتماداً على رسائل الخطأ القديمة أدناه.
- استخدمت النسخة التسجيلات المحسّنة نفسها من أرشيف FLAC؛ لا إعادة توليد صوت في خطوة النشر الأخيرة. مراجع FLAC في `voice07/script.json` ويمكن استعادتها بـ`materialize_lossless_takes.py`.
- 9 تسجيلات، 32 موضع سرد؛ 28 موضعاً بالسرعة الطبيعية، لا إبطاء، وأقصى تسريع نحو 7.2%. ملف صوت وسيط غير مضغوط 48 kHz للمزج، وخلفية أصلية مستخرجة آلياً ومنخفضة تحت السرد.
- الصورة كاملة دون إعادة ضغط: 20288 حزمة مطابقة للمصدر. جهارة −16.1 LUFS وذروة −1.99 dBTP. نجح 66 اختباراً مستهدفاً. هذه فحوص تقنية وليست ادعاءً بمراجعة سمعية بشرية أو ضمان مشاعر محددة.
- المعاينة غير المكتملة القديمة لـSnaptik أُرشفت بمرجع Git ثابت متحقق منه في `preview-archive.json` لتقليل حجم الحفظ؛ لم تتغير النسخ النهائية أو التسجيلات السابقة.
- حجم شجرة التغييرات الحالية يقارب 125.2 MB (عشري). لا تضف WAV/MP3/MP4 مكررة كبيرة؛ استخدم الأرشيفات و`.cache/` للوسائط الوسيطة.
- العمل كله على `arena/01a07c92-dub`؛ لا دمج أو إغلاق PR. الحالات التالية أرشيف سير العمل، وليست الحالة الحالية.

## آخر اختيار صوت — voice-07 من أول مجموعة في دفعة العشر عينات

- طلب المستخدم الصوت الذي اختاره من المجموعة الأولى في آخر اقتراح (الأزواج index 7–11). المقصود **voice-07**، وليس voice-00 أو voice-04 أو آخر زوج.
- أُنتجت عينة أطول من القصة في `dubs/hajj-dream-2108415/voice-tests/voice-07-natural.wav`، مباشرة من أداة الصوت، دون تسريع أو مؤثرات أو معالجة إضافية.
- **في انتظار اعتماد نطق العينة الأطول قبل التسجيل الكامل.** لا تنشر أو تعِد تسجيل فيديو 11 دقيقة بناءً على اختيار الزوج وحده. الحالة `awaiting_voice_pilot_review` ومرشح الصوت `voice-07`.
- التسجيلات القديمة الفعلية تبقى بصوت `voice-02`، ولم تتغير هويتها أو تُحذف. voice-07 مسجل في هذه الجلسة فقط.

## آخر اختبار صوت — voice-04، في انتظار رأي المستخدم

- رفض المستخدم نطق `voice-02` في التسجيلات الطويلة ثم رفض عينة `voice-03` أيضاً.
- عُرض زوج جديد (index 4، رجالي ar-EG للسرد) واختار المستخدم `voice-04`. أُنتجت عينة أطول فقط، من القصة وبجمل قصيرة، في `dubs/hajj-dream-2108415/voice-tests/voice-04-natural.wav`.
- ملف WAV هو خرج أداة الصوت دون تسريع أو مزج أو معالجة إضافية. لا تدّع أن نطقه طبيعي قبل موافقة المستخدم.
- **لا توليد جماعي ولا نشر فيديو نهائي حتى اعتماد العينة.** تسجيلات `voice-02` القديمة محفوظة ولم تتغير هويتها. الحالة الحالية `awaiting_voice_pilot_review`.

## تنبيه جودة الصوت — لا تنشر نسخة الحج الحالية

- أبلغ المستخدم أن النطق سيئ وآلي. اعتماد التسجيلات الحالية متوقف؛ لا تعادل سلامة التوقيت والملفات جودة نطق طبيعية.
- الصوت المستخدم `voice-02` من أدوات الوكيل. المقاطع الظاهرة من generate_speech خام قبل المزامنة؛ إن كان المقصود الفيديو فالتسريع/الإبطاء قد يزيد الأثر أيضاً. لم يُحسم أي نسخة سمعها المستخدم، فلا تنسب السبب قطعاً إلى السرعة أو الضغط.
- لا تعِد التوليد بالجملة ولا تنشر فيديو نهائياً قبل اعتماد عينة حقيقية قصيرة بسرعة طبيعية. احفظ الملفات السابقة ولا تحذفها.
- التسجيلات التسعة و12 قطعة خلفية محفوظة في GitHub على فرع الجلسة. التفاصيل في `dubs/hajj-dream-2108415/voice-review.json`، والحالة `paused_pronunciation_review`.

# ذاكرة المشروع — ProStudio (DUB-)

## سياسة الصوت في جلسة أرينا الحالية

الصوت **يولّده الوكيل** عبر `add_voice` ثم `generate_speech` (صوت مصري رجالي احترافي افتراضياً).
أدوات TTS الأخرى (Edge / VoxCPM / XTTS / eSpeak) **ليست** مصدر الصوت في هذه الجلسة.

المستودع (`zaldynbwtany202-dev/DUB-`، فرع الجلسة الحالي `arena/01a07c92-dub`) بيئة العمل فقط:

1. تفريغ وترجمة وتقطيع زمني إن لزم.
2. ورقة إشارات `cues.json` عبر `python scripts/agent_dub.py plan …`
3. الوكيل يسجّل كل سطر في `agent_voice/seg-XXXX.mp3`
4. المزامنة والمزج والمكساج: `python scripts/agent_dub.py sync --cues … --output …`

## فيديو جديد مستلم — hajj-dream-2108415

- المستخدم أعلن رفع فيديو جديد. تم العثور عليه في فرع Sendbox `arena/01a07c69-dub`، بالكوميت `2108415357eb076ec7404036baa6d81bb04995f3` (2026-09-07T20:35:04Z). جرى قراءة هذا الفرع فقط، والعمل ما زال على `arena/01a07c92-dub`.
- **المصدر الجديد**: `library/hajj-dream-2108415/source.mp4`، عنوانه «رجل كبير في العمر يحلم ان يحج بيت الله (الكعبة) فيبيع كل ابقاره وممتلكاته لكي يجمع المال، لكن!».
- حجمه **39,320,203 بايت (37.5 MiB)**، مدته **676.327619 ثانية (11:16 تقريباً)**، ‏854×480، ‏30 fps، H.264 وصوت AAC ستيريو 44.1 kHz.
- رُفع على 3 أجزاء. تم تنزيلها، والتحقق من SHA-256 لكل جزء وبصمات كائنات Git، ثم جمعها دون إعادة ضغط، والتحقق من الحجم وفك ترميز الملف كله دون أخطاء. البصمة الكاملة: `26a4dce57e2a780c4e32d57d13ea53d1bf01c8edc562680ca1561c04540cebb4`.
- دليل الأصل والتجميع في `upload-provenance.json` و`upload-manifest.json` بجوار الفيديو. الإدخال محفوظ كملف واحد على فرع الجلسة، دون حذف الأجزاء من فرع الرفع.
- **لم يبدأ تفريغ هذا الفيديو أو دبلجته بعد**. لا تعرض ناتج Snaptik القديم على أنه ناتج الفيديو الجديد. طلب المستخدم أصواتاً جديدة؛ اختار `voice-01` في الجولة الأولى ثم طلب بدائل واختار `voice-02` في الجولة الأحدث (`ar-EG`، رجالي، narration، index 2). هذا آخر اختيار من المعاينات، محفوظ في `voice-selection.json`؛ لم يُولّد تسجيل الفيديو الكامل بعد.
- هذا الفيديو أطول بكثير. استخدم معالجة محدودة الذاكرة/مقسّمة للفصل والمزامنة؛ STFT لكامل 11 دقيقة قد يتجاوز الذاكرة. لا تُضف أوزان النماذج أو WAV/أطياف وسيطة كبيرة إلى Git، وتجنب تكرار الفيديو في عدة مخرجات ضخمة.

## الفيديو السابق — كامل وبخلفية الأصل (2026-09-07)

- **الفيديو الأحدث**: `dubs/snaptik-7614635067838598423-v3/final-dub-agent-ar-eg-original-bg.mp4`، مدته **108.9 ثانية**، بنفس الصوت المصري الرجالي `voice-00` الذي اختاره المستخدم، مع الخلفية المستخرجة من الفيديو الأصلي.
- اختار المستخدم **استرجاع الموسيقى الأصلية** وليس تأليف موسيقى جديدة. نجح نموذج UVR ONNX على CPU، بأوزان مثبتة في `config/uvr-background.json`، وخلفيته في `background/separated-original-background.flac`. لم يُولَّد كلام جديد في مرحلة الموسيقى.
- الخلفية **تقدير مفصول آلياً وليست stem استوديو مضمونة النقاء**. لم يسترجع فحص ASR عبارة موثوقة من الراوي القديم، لكن ذلك لا يضمن انعدام كل البقايا. تقرير الفحص بجوار الخلفية.
- الموسيقى +2.5 dB قبل الخفض التلقائي أثناء الكلام (ratio 3:1، attack 20 ms، release 500 ms)، مع دخول وخروج تدريجيين. دليل المعلمات وإعادة التشغيل في `docs/ORIGINAL_BACKGROUND_AR.md` و`background-mix.recipe.json`.
- **النسخة بدون موسيقى محفوظة دون تغيير**: `final-dub-agent-ar-eg.mp4`. استخدمها هي مصدر الكلام عند إعادة المزج، ولا تُضف الخلفية فوق نسخة تحتوي موسيقى بالفعل.
- جميع التسجيلات التسعة موجودة، ولا مقاطع ناقصة. `state.json` مكتمل ويشير إلى نسخة الموسيقى؛ الصوت المنفصل وSRT محفوظان بجانب كل إصدار. المعاينة القديمة ليست أحدث نتيجة.
- فحص نسخة الموسيقى: الصورة الأصلية كاملة (3267 حزمة مطابقة)، 108.9 ثانية، AAC ستيريو 48 kHz، −16.01 LUFS و−3.33 dBTP، ولا أخطاء فك ترميز. توقيت الكلام لم يتغير في ست نوافذ مقاسة، ونجح **62 اختباراً مستهدفاً**. لا ادعاء بمراجعة سمعية بشرية أو مزامنة شفاه.
- الكود الجديد: `youtube_auto_dub/neural_background.py` و`scripts/separate_original_background.py` و`scripts/mix_original_background.py`. المتطلبات الخفيفة في `requirements-background.txt`. الأوزان وبيانات الفصل الكبيرة داخل `.cache/` فقط؛ لا تضفها إلى Git.
- الشغل على `arena/01a07c92-dub` فقط. لم يُدمج أو يُغلق أي PR، ولا يُفترض نشر Pages من هذا الفرع تلقائياً.
- اختيار `voice-00` مسجّل في هذه الجلسة فقط. إعادة مزج الملفات الموجودة لا تحتاج اختيار صوت أو TTS جديداً.

## تنفيذ التفريغ السابق — 2026-09-07

- الفيديو المقصود هو `library/snaptik-7614635067838598423-v3/source.mp4`، حجمه 7,967,406 بايت، وليس الفيديو التجريبي.
- نجح تفريغه محلياً بنموذج Whisper Small متعدد اللغات و`whisper.cpp v1.7.6`: العربية، 34 مقطعاً و308 كلمات. النتائج في `source.txt` و`source.srt` و`source.segments.json` بجوار المصدر.
- **التفريغ مسودة للمراجعة قبل التسجيل**، خصوصاً توقيت 20.560–39.800 ثانية. التفاصيل في `source.review.md`.
- تثبيت الحزمة وحده لم يكفِ: Hugging Face ومضيف أصول Releases تعذّر الوصول إليهما. نجح تنزيل أجزاء GGML من كائنات GitHub العادية والتحقق من تطابق النموذج مع البصمة الرسمية.
- إعادة التجهيز: `python scripts/bootstrap_local_whisper.py`؛ التشغيل: `python scripts/transcribe_video.py --backend whisper-cpp --source … --output-dir output/asr`. المتطلبات والخطوات في `docs/ASR_LOCAL_AND_GITHUB_AR.md`.
- اتصال GitHub والرفع يعملان. رفض إنشاء Workflow بسبب غياب صلاحية `workflows`، **وليس لأن الجلسة أُغلقت**. قالب Actions محفوظ في `docs/github-actions/transcribe.yml` وغير نشط هناك.
- لم تُشغّل أدوات TTS ولم يُولّد صوت ولم يُدمج أو يُغلق أي PR في هذا التنفيذ. لا تفترض أن اختيار صوت من جلسة سابقة مسجّل في الجلسة الحالية؛ يلزم `add_voice` قبل `generate_speech`.
- الأوزان والبناء والبيئة داخل `.cache/` و`.venv/` مستثناة من الحفظ بين الجلسات. أعد تجهيزها بالسكربت، ولا تضفها إلى Git.

# ذاكرة المشروع — ProStudio (أرشيف المصدر)

> **ما يلي أرشيف مستورد لمشروع/جلسات قديمة، وليس تعليمات الفرع الحالي أو النشر الحالي. لا تتبع أسماء الفروع أو قاعدة الدمج القديمة أدناه.**

> آخر مراجعة موثّقة: **2026-08-29** — كل رقم في هذه المذكرة مُخرَج من أمر `git`/`gh`/`pytest`
> نُفِّذ فعلاً في هذا التاريخ، وليس تقديراً.

## قاعدة العمل الدائمة

`arena/01a03969-prostudio` هو **الفرع الدائم للمستخدم** وهو **مصدر GitHub Pages**
(المسار `/docs`). أي تعديل يجب أن يصل إليه حتى يظهر على الموقع.

**لكن**: جلسة أرينا الحالية **مربوطة بمنصة التشغيل** على فرع الجلسة
`arena/01a04ec9-prostudio`، ولا يمكن للوكيل تحويل الجلسة إلى فرع آخر — هذا قرار
المنصة وليس اختياراً. لذلك سير العمل الإلزامي هو:

1. العمل على فرع الجلسة `arena/01a04ec9-prostudio`.
2. `git push origin arena/01a04ec9-prostudio`.
3. فتح PR منه إلى `arena/01a03969-prostudio` ودمجه.
4. GitHub Pages يعيد البناء تلقائياً من `arena/01a03969-prostudio:/docs`.

قبل أي تعديل: `git fetch` ثم `git merge --ff-only origin/arena/01a03969-prostudio`
للتأكد أن العمل يبدأ من آخر ما دفعه المستخدم، لا من نسخة قديمة.

## الهوية والروابط (مؤكّدة بـ `gh api` بتاريخ 2026-08-29)

| الشيء | القيمة |
|---|---|
| المستودع | https://github.com/dhiyaddineb-hue/prostudio |
| فرع المستخدم | https://github.com/dhiyaddineb-hue/prostudio/tree/arena/01a03969-prostudio |
| **الصفحة المنشورة** | **https://dhiyaddineb-hue.github.io/prostudio/** |
| صفحة إرسال ملف | https://dhiyaddineb-hue.github.io/prostudio/send.html |
| معاينة مقطع | https://dhiyaddineb-hue.github.io/prostudio/preview.html?p=<اسم-المشروع> |
| مصمم الأصوات | https://dhiyaddineb-hue.github.io/prostudio/studio.html |
| إعداد Pages | الفرع `arena/01a03969-prostudio`، المسار `/docs`، `build_type: legacy`، الحالة `built` |
| آخر بناء Pages | من الكوميت `ea677a7` في `2026-08-29T18:32:34Z` — ناجح |

## ما تم فهمه من المراجعة

ProStudio استوديو عربي لدبلجة وترجمة فيديوهات YouTube والملفات المحلية. المسار
الأساسي: التنزيل أو الرفع ← التفريغ ← الترجمة ← توليد الصوت ← مزامنة الصوت ومزج
الخلفية ← تصدير MP4 وSRT.

- **النواة** Python داخل `youtube_auto_dub/` — `core.py::run()` هو الخط الأنبوبي.
- **الواجهة المحلية** FastAPI داخل `web/app.py` على المنفذ 8080 (`0.0.0.0` افتراضياً،
  مع `proxy_headers=True`) — تُشغَّل بـ `python run_studio.py`.
- **واجهة GitHub Pages** ثابتة داخل `docs/` — تعرض الدبلجات الجاهزة، وترفع الملفات
  مباشرة عبر GitHub API (`github-upload.js`)، وتضم مصمم الأصوات.

### أرقام مرجعية (مُخرَجة من `git ls-files` بتاريخ 2026-08-29)

| المكوّن | العدد |
|---|---|
| الملفات المتتبَّعة في Git | **216** |
| وحدات Python في `youtube_auto_dub/` | **30** (+ `language_map.json` = 31 ملفاً) |
| ملفات `web/` | **11** |
| سكربتات `scripts/` | **13** |
| ملفات `tests/` | **31** (26 وحدة اختبار Python + `__init__.py` + 4 ملفات `.mjs` للمتصفح) |
| مشاريع الدبلجة | **4**: Bob-Proctor-5min-2v، Bob-Proctor-Sample، Phantom-Thread، Vikings-Ragnar-Floki |

### نتيجة الاختبارات (نُفِّذت فعلاً بتاريخ 2026-08-29)

```
.venv/bin/python -m pytest -q     →  171 passed, 3 skipped, 0 failed
node tests/browser/*.mjs          →  32 + 12 + 26 + 36 = 106 تأكيداً، 0 فشل
```

ملاحظة: اختبارات `.mjs` **ليست** حزم `node:test` — تُشغَّل مباشرة
(`node tests/browser/shrink.test.mjs`)؛ `node --test tests/browser/` يفشل.

## حدود طلب اللعبة الحالي

الرسائل الحالية تطلب فهم المشروع وتثبيت الفرع والروابط في الذاكرة، ولا تقدم تصميم
لعبة أو آليات لعب أو هدفاً بصرياً. لذلك لا ينبغي إنشاء لعبة أو تحويل المشروع إلى
Babylon/WebDev دون وصف إضافي صريح؛ عند وصول وصف لعبة لاحقاً، يجب أولاً قراءة مهارة
تطوير الألعاب ثم بناءها في نطاق منفصل دون كسر خط الدبلجة الحالي.

## سياسة المزامنة على كلام الأصل (أُضيفت 2026-09-09)

- **الفجوات ليست وقفات مقصودة دائماً**: إذا سُجِّل التعليق كاملاً ووُضع في أول نافذته،
  بقي صمت حيث يتكلم الأصل. معيار القبول هو تغطية **زمن كلام الأصل**، لا مجرد طول الملف.
- **الأداة**: `scripts/sync_takes_to_source_timeline.py` تقطّع كل تسجيل عند مواضع صمته
  المقاسة (Silero)، وتضع كل جملة عند بداية الوحدة الكلامية في الأصل، وتوزّع الفائض على
  الوقفات داخل `[0.12, 0.70]` ثانية. **سرعة الكلام 1.000× دائماً** ولا يُقتطع كلام مسموع.
- **لا يُبرَّر الصمت بالموسيقى**: الخلفية تُستعاد من المصدر وتُخفض أثناء الكلام، ولا
  تُستخدم لملء فجوات الكتابة.
- **بوابة التسليم** (`scripts/verify_source_sync_render.py`) ترفض أي نسخة ما لم تتطابق
  كل قطعة مع التسجيل المعتمد عيّنة‑بعيّنة، ويقلّ خطأ الموضع عن 3 عينات، ولا تتجاوز أطول
  فجوة 0.75 ث، وتبقى الصورة منسوخة (MD5 تيار الفيديو مطابق للمصدر)، والجهارة −16 LUFS.
- **الفحص الرقمي لا يعفي السمع**: النطق واللهجة والانفعال تُحكم بقرار المستخدم؛ وتُذكر
  حدود النص صراحة (كثافة الكلمات مقابل الأصل) بدل الادعاء بتطابق لفظي كامل.
## سياسة المزامنة النهائية: توجيه كل جملة إلى موضعها في الأصل (2026-09-09)

شكوى المستخدم بعد نسخة «إعادة توزيع الوقفات»: *المزامنة لا تزال سيئة*. القياس أثبت السبب:
تعبئة النافذة كانت تُبقي صمتاً مسموعاً أثناء كلام الأصل (35 فجوة أطول من 0.7 ث، أقصاها
1.58 ث) ولا تضع أي جملة حيث يقول الأصل محتواها المقابل.

**الطريقة المُطبَّقة** (`youtube_auto_dub/sentence_anchor.py` + `scripts/sync_takes_to_source_timeline.py`):

1. `source_units` تقسّم كلام الأصل في النافذة إلى وحدات بعدد جُمل الدبلجة، والفصل على أنفاس
   الأصل داخل نافذة محلية حول الرتبة المثلى (المقارنة بدقة المللي‑ثانية حتى لا تتفوق ضوضاء
   الفلو‑بوينت على المحلية).
2. `sentence_indices` في `pause_sync` تحدد أي مقطع من التسجيل يبدأ كل جملة (جملة واحدة لا
   تُقسَّم أبداً)، ومنها تُشتق `align_sentences` (اتجاه واحد للحساب، لا نسختين من المنطق).
3. `spread_gaps` يُسقِط الوقفات المطلوبة على الصمت الذي يملكه الجزء: حد أدنى يمنع التراكب،
   حد أقصى `--anchor-hole` يمنع الصمت أثناء كلام الأصل، والمجموع = الصمت المتاح بالضبط فلا
   فجوة عند نهاية الجزء. الاقتران **بالترتيب وكتلة الكلام** لا بتشابه النص (المطابقة اللفظية
   ~35% فقط بين الصياغة المصرية والنص المنطوق) — لا تُحاول المطابقة النصية هنا.
4. `--min-speech 0.3` يدمع الجزر الكلامية القصيرة (0.1 ث) في القطعة السابقة: كيان واحد قصير
   كان يخلق فجوة مسموعة 0.77 ث في النص المسلَّم السابق.

**نتيجة القياس على الملف المسلَّم** (`dubs/hajj-dream-2108415/source-sync/`): الفجوات أثناء
بالأداة نفسها: السابق {فجوات 170، مجموع 59.078 ث، أقصى 1.58 ث، 35 فجوة > 0.7 ث}،
الحالي {269 فجوة، 38.082 ث، أقصى **0.45 ث**، **صفر** فجوة > 0.7 ث}؛ التغطية 97.08% جاف /
96.30% مكساج؛ 237 قطعة مطابقة عيّنة‑بعيّنة بخطأ موضع 0.0454 مللي‑ثانية؛ البوابات التسع
`verified`.

**الحد المكشوف (لا يُخفى بعد اليوم):** خطأ موضع الجُمل الباقي متوسطه 2.937 ث (وسيط 2.234،
p90 6.702، أقصى 14.499). 96 مرساة «مستحيلة» (من `anchor_conflicts` في الخطة) من أصل 236 صفاً موضعياً،
أي أن وقتها يمرّ قبل أن تنتهي الجملة السابقة، لأن جمل الدبلجة أقصر من مدد الأصل: 1510 كلمة
مقطوعة مقابل 1855 كلمة بتوقيتها، وميزانية الإعادة 1634 كلمة لـ 288 وحدة
(`timing-repair/clip-budget.json`). لا حركة صمت ولا خوارزمية تسدّ هذا؛ المفاضلة مقاسة في
`timing-repair/policy-sweep.json` (2026-09-09): رفع سقف الصمت من 0.25 إلى 0.75 ث يحسّن المتوسط
من 2.975 إلى 2.724 ث فقط، مقابل صمت مسموع أطول (أكبر فجوة 0.45 → 0.58 ث). نُشر السقف 0.5 ث.
الصمت المتاح 73.801 ث يغطي طلب المراسي 68.192 ث على مستوى الملف، والعائق توزيعه المحلي في الأجزاء 1, 2, 3, 5, 7. العلاج المتبقّي نصّي: صياغة بكثافة الأصل ثم تسجيل من جديد. أي README يقول «كل جملة عند موضعها»
دون حقول `anchor_target`/`anchor_error` ومقياس `quiet_holes` **مُبالغة** — لا تُكررها.

### 2026-09-09 — الأصل بلا وقفات: الجذر الحقيقي لشكوى «المزامنة سيئة»

لقياس وقفات الأصل الحقيقية لا يُمكن الاعتماد على VAD طاقي على المكساج (السرير الموسيقي
مستمر فلا صمت) ولا على `words.json` (أغلفة DTW تدمج الأنفاس: فجوتان فقط ≥ 0.35 ث في 676 ث).
الطريقة الصالحة: `scripts/measure_source_vocal_pauses.py` يطرح تقدير الخلفية العصبي بعد ملاءمة
مقياسه (5.19 — لهذا يرفع المكساج الخلفية +12 dB) ثم يقيس الغلاف على البقايا الصوتية.
النتيجة (`timing-repair/source-analysis/vocal-pauses.json`): **125 مقطع كلام، 124 وقفة،
متوسطها 0.161 ث، مجموع صمت الأصل 19.99 ث من 676.33 ث (97% كلام متصل)**.

الاستنتاج الهندسي: أي وقفة في الدبلجة ليست عند وقفة الأصل = سقوط مسموع للصوت فوق كلام الأصل؛
ومع 237 قطعة (1510 كلمة) عندنا ~35 ث صمت مقابل 20 ث عنده — جزء ملموس من «سوء المزامنة» هو
هذا لا التخطيط. `spread_gaps` لا يستطيع التقليل: مجموع الوقفات = صمت مسروق من التسجيلات
(73.8 ث) محفوظ وجبريًا موزّع، والسقف يُرفع تلقائيًا عند الضيق؛ لذلك `--anchor-hole` لا يغيّر
المجموع إلا قليلًا (34.10→34.90 بين 0.28 و0.5) — والمفاضلة الحقيقية في **عدد الجُمل**.

**المخطط الثالث (المطلوب الآن):** `scripts/build_phrase_budget.py` →
`timing-repair/phrase-budget.json`: جملة دبلجة واحدة لكل مقطع كلام في الأصل — **107 جملات،
1585 كلمة، ~14.8 كلمة/جملة، صمت مستهدف ≈ 25 ث**، فالعدد 1:1 مع المراسي يلغي الاستيفاء
ويعطي طورًا مقاربًا للصفر بالبناء لا بالتقدير. صياغة الجولة الثالثة تُبنى بدمج جُمل
`script-dense-source.json` المتجاورة داخل المقطع الواحد (وحذف الوقفات الداخلية)، لا بكتابة من
الصفر؛ والميزانية لكل جملة في `phrase-budget.json.rows[].needed_dub_words`.
`measure_clause_fit.py` صار يقيس لكل **قطعة** (عدد القطع ≠ عدد الجُمل، و`sentence_indices`
ينهار إلى 0 حين تزيد الجُمل على القطع — لا تُعِد استخدامه لتقييم الجُمل مباشرة).

### 2026-09-09 (مساءً) — لا موسيقى في المصدر: طبقة «الخلفية» حُذفت

ملاحظة المستخدم: «تضيف الموسيقى من عندك مع إن المقطع بدون موسيقى». القياس يؤكدها:
تقدير الفصل العصبي (`background.wav` المستعاد من `background/parts`) طاقته **90.8% داخل
نطاق الكلام (120 Hz–1 kHz)**، و1.3% فقط فوق 1 kHz، ومستواه **−45 dBFS rms أي 28 dB تحت
المكساج**، ومداها الديناميكي يتبع الجُمل — هذه بقايا صوت الراوي الأصلي + صمت الغرفة، لا
موسيقى. رفعناها بـ+12 dB (×4) فصار تحت الدبلجة صوتٌ عربي ثانٍ يهمس — وكان يُخفي الفجوات
أيضًا، لذلك كانت `delivered_max_gap` تقاس 0.64 ث بينما الجاف 0.45 ث.

الفعل: `scripts/mix_original_background.py --no-background` (مُضاف الآن، والرسول اختياري
بينه وبين `--background`)، والمسلَّم صار تعليقًا وحده. النتيجة مقاسة على الملف الجديد:
`delivered_coverage` **0.9818** (كان 0.9732) و`delivered_max_gap` **0.42 ث** (كان 0.64)،
و9/9 بوابات `verified`، والصورة مطابقة، والشا `8b82081c…`، والحجم 41,915,527 بايت.
النسخة ذات الطبقة محفوظة في `archive-with-bed/` للمقارنة السمعية فقط، وسبب الاستبدال مسجّل
في `meta.json.archived_versions`.

**أصوات هذه الجلسة:** معرّفات الصوت خاصة بالجلسة (voice-02 هنا ≠ voice-02 المرفوض سابقًا).
المستخدم طلب «صوتًا أثخن قليلاً» واختار بعد جولتي تنافس `voice-02`. عينات حقيقية من نص
الدبلجة محفوظة في `dubs/hajj-dream-2108415/voice-tests/voice-02-s{1,2,3}-*.{flac,mp3}` مع
`voice-02-thick-samples.json` (مدد 17.3/21.7/18.6 ث، ذروة −3.4…−3.0 dBFS). **مقياس مهم
للجولة الثالثة: سرعة voice-02 مقاسة 1.89–2.19 كلمة/ثانية (24 kHz)** — أبطأ من voice-12
(2.44) وvoice-00 (2.507)، فميزانية `phrase-budget.json` أُعدّت على 2.507؛ عند الاعتماد النهائي
لـvoice-02 أعِد حسابها بنحو 1270 كلمة لـ632 ث كلام الأصل (وليس 1585).

### 2026-09-09 (ليلًا) — «الصوت متصنع»: ما يُقاس وما لا يُقاس

ردّ المستخدم على عينات voice-02: «الصوت جيد لكن نطق اللهجة سييء جدًا»، ثم «أريد صوتًا
احترافيًا بشريًا». ما ثبت بالقياس على هذا المشروع:

1. **مقاييس «البشرية» التقليدية هنا عمياء**: jitter النسبي (20.1% للأصل مقابل 22.5–24.2%
   للمرشحين) وshimmer (35.4% مقابل 31–36%) متقاربة جدًا — أي أن الاضطراب الدوري للدورانية
   التلقائية لا يفرّق بين مسجَّل بشري وتوليد، فلا تصلح حُكمًا ولا بوابة. لا تبنِ عليها قرارًا.
2. **ما يفرّق فعلًا وهو قابل للإصلاح من غير إعادة تسجيل** (`scripts/match_source_acoustics.py`
   → `timing-repair/acoustics-match.json`): أرضية ضجيج الأصل **−31.6 dBFS** مقابل **−63.3**
   للماستر (صمت رقمي بين الجُمل = «ملصوق»)، وطاقة تحت 200 Hz **16.7%** مقابل **46%** (صدر
   زائد على ميكروفون لا يحتمله)، والميل الطيفي **−28.25** مقابل **−30.97** dB/عقد (أغمق =
   مكتوم). السلسلة المُطبَّقة: `bass −4.39 dB@180`، `treble +2.72 dB@3.2k`، `lowpass 6.5k`،
   ثم صمت غرفة الأصل نفسه (مأخوذ من وقفات الأصل، لا مُلَفَّق) بمستوى −31.57 dBFS.
   النتائج بعد المعالجة: فرق الأرضية **−1.15 dB**، فرق الميل **−1.02**، فرق الطاقة فوق 6k
   **+0.046%**. الأشرطة المرجعية `voice-tests/acoustics-ab-*.mp3` (0–60 ث و411.8–471.8 ث).
3. **الحكم ما زال سمعيًا**: معالجة الطابع/اللهجة ليست مسألة EQ. قائمة الاختبار صارت قاعدة
   مستودع: `voice-tests/dialect-stress.txt` (ست جمل تفخخ الميكانيكية الفصحى: ق/ج/ك، «مش/
   عشان/إزاي/دلوقتي»، صيغ الحج، نفي بلا إعراب). **لا صوت يُعتمد قبل سماع الست كاملة**،
   وعيّنات مقارنة محفوظة: `voice-tests/voice-0{0,1,3}-c*.{flac,mp3}` مع `dialect-check.json`.
   voice-02 رُفض بهذه القائمة. voice-01 فيه c1,c2 فقط (قُطع توليد c3).
4. **قيد يجب التصريح به للمستخدم**: مصدر الصوت الوحيد المسموح في هذا Setup هو صوت المنصة عبر
   `generate_speech`؛ لا XTTS/CozyVoice/Edge-TTS (قاعدة المستخدم)، وتنزيل أوزان من HuggingFace
   **مستحيل في هذه البيئة** (TLS يُغلق؛ `pip` فقط يعمل). «صوت بشري احترافي» يعني إما اختيار أنسب
   صوت متاح بالقائمة أعلاه، أو أن يرفع المستخدم تسجيلًا بشريًا من `docs/send.html` ويتولى
   المستودع المحاذاة/المكساج/التحقق (البنية جاهزة: takes + sha256 + verifier). لا تقل له
   «سأجعل التوليد بشريًا» — لا تفعل ذلك إلا بمعالجة الخصائص أعلاه وبصوت يقبله سمعه.
5. **بوابة جديدة مطلوبة قبل نشر أي ماستر مُعالَج**: `verify_source_sync_render` يشترط تطابق
   القطع عيّنة-بعيّنة مع الـTake؛ سلسلة EQ+ضجيج تكسر ذلك شرطيًا. عند النشر: أضف وضعية
   `--master-chain acoustics-match.json` تعيد تطبيق السلسلة على القطع وتقارن الناتج
   بالمسلَّم (تطابق تام متوقع)، بدل فرض `speech_untouched` على الصوت الخام. لا تنشر نسخة
   المعالجة كبوابة خضراء قبل كتابة هذا الوضع.

### 2026-09-09 (مساءً) — إعادة بناء كاملة من GitHub + بوابة «الهواء مش موسيقى»

**الساندبوكس اتصفّر في النص (11:42)**: لا venv، لا `.cache`، وHEAD رجع لوراء `9048ea7`
والشجرة فيها ملفات أحدث/أقدم مخلوطة. الاسترجاع الصحيح (اتعمل فعلاً وانجح):

```
git fetch origin arena/01a08487-dub          # shallow: بالـsha مش بالاسم لو لزم
git reset -q --mixed <head-المنشور> && git status --porcelain   # طابِع الأول: إيه الفرق فعلًا
git reset --hard <head-المنشور>
python3 -m venv /home/user/dubenv && /home/user/dubenv/bin/pip install -q \
    numpy soundfile imageio-ffmpeg faster-whisper pytest
```

1. **كل المادة الخام موجودة في المستودع نفسه**: `library/hajj-dream-2108415/source.mp4`
   **متعقَّب** رغم `*.mp4` في .gitignore (استثناء المقبولات) وبصمته `26a4dce5…0cebb4` ✓.
   السرير الصوتي بيتبني من الأجزاء المتعقَّبة في `3 ثواني`:
   `python scripts/restore_background_parts.py --source library/<slug>/source.mp4 --parts
   dubs/<slug>/background/parts --output .cache/<slug>-sync-repair/background.wav`.
   والتسجيلات المعتمدة بترجع بـ `scripts/materialize_lossless_takes.py --script
   dubs/<slug>/timing-repair/script.json` (من كوميتات الأرشيف، من غير أي إعادة توليد).
2. **VAD هو مشكل الريفريش**: `silero_spans` في `sync_takes_to_source_timeline.py` بيستورد
   `faster_whisper.vad` (نموذج ONNX مرفق مع الحزمة، بلا تنزيل من HF). من غير `pip install
   faster-whisper` بيرجع [] والـfallback هو `energy_spans` → الكلام بيبدو أطول و`uncovered`
   يقفز 11.74 → 29.34 ث ويفشل البتة (`ValueError`). لو شفت الرقم ده في sandbox جديد، ده السبب،
   مش نص سييء.
3. **إعادة البناء مش bit-identical، وسببها مفهوم**: بلان المعاد بناجه يطابق المنشور حرفيًا في
   `source_speech_coverage=0.982609` و`uncovered=11.74` و`max_uncovered_gap=0.0`، لكن بصمة
   الماستر بتختلف (`4674f021…` مقابل `2ae18fb5…` المسجلة في mix.json) لأن FLAC المؤرشف
   بيتعمله resample من 24k بشكل مختلف عن WAV العائم للأداة، فحدود القص داخل كل take اتغيرت
   (median 0 ms، p90 194 ms، 61/237 أكتر من 60 ms). **النتيجة العملية**: `--plan` لازم يكون
   من نفس الريان اللي بنى الماستر؛ اعملهم ورا بعض في نفس الجلسة (وهذا اللي هيحصل في الجولة
   التالتة)، ومفيش داعي تتوقع إن بصمة قديمة تتكرر.
4. **`verify_source_sync_render.py` بقى ليه وضع `--master-chain`** (مع `--dry-narration`):
   بيعيد تطبيق السلسلة المعلنة في تقرير `match_source_acoustics.py` على الماستر الجاف، وقياس
   **الفرق في الوقفات بس** (مش على الملف كله — عشان أي loudnorm ما يسرقش الرقم)، ولازم
   يبقى: إزاحة زمن 0، طول مطابق، انتشار مستوى ≤2 dB/ثانية، فرق «وقت الكلام − وقت الوقفة»
   ≤4 dB، flatness ≥−6 dB، peakiness ≤12 dB، ومستوى ضمن ±4 dB من `room_tone_target_dbfs`.
   البوابة `master_chain_is_air_not_music` + دالة `master_chain_gate(mc) -> bool` + 6 اختبارات
   تركيبية (`tests/test_master_chain.py`) تثبت إن البوابة بتقبول الهواء و**بترفض**: همّ 220 Hz
   عند −23 dBFS، سرير بـducking (الفرق −26 dB)، نسخة متأخرة 10 ms، وهوا بأعلى +9 dB.
   ملاحظة معايرة: `correlation_with_speech_envelope` لوحدها بترضّ ~0.2 مع ضجيج أبيض
   (n=32) فوسعنا حدودها لـ0.5؛ الكاشف الحقيقي للـducking هو فرق المستوى، مش الارتباط.
5. **مقاييس «بشرية» اللي انثبتت عمياء اتمسحت من الترجيح** و`scripts/compare_voice_candidates.py`
   بيقيس الطرفين من نفس النافذة 16 kHz وعلى نفس الإطارات المصوتة؛ `level_sd_db` لازم يُقاس
   على الكلام فوق `floor+6 dB` بس، وإلا الوقفات بتخلي أي عينة تبان «أكثر حيوية» كذبًا.
   معرض `dubs/hajj-dream-2108415/voice-tests/gallery/`: 10 مرشحين قرأوا نص حقيقي،
   `pace-fit.json` بيحوّل السرعة لثمن: المطلوب 1585 كلمة فوق 632.5 ث؛ `voice-07` (2.545 ك/ث)
   و`voice-13` (2.509) و`voice-11` (2.516) بس اللي يبلعوا النص كامل؛ الباقي معناه قطع كلمات.
   `voice-06` أنضف نطاق (0.161 تحت 200 Hz مقابل 0.151 للبشري)، `voice-08` أقرب حركة مستوى.
   **القرار للمسمع**: المستخدم يقول رقم، وبعدها الجولة التالتة مباشرة من غير أسئلة.

### 2026-09-09 (ليلًا) — الهواء مرفوض من المصدر، والماستر المطابق «verified»، والدفعة معلّقة على توكن

1. **`vocal-pauses.json` فخّ**: `spans` = وحدات **كلام** الأصل (125، متوسط 5.248 ث)، والوقفات
   الحقيقية في `pause_list` (124، متوسط 0.161 ث، الإجمالي 19.99 ث). قراءة المفتاح الغلط كانت
   بتحط صوت الراوي الأصلي نفسه تحت الدبلجة عند −25.9 dBFS بنفس ذروته 215 Hz.
   `pause_spans()` بقى يختار بالشكل (متوسط < 1 ث)، وبيقاطع نوى كلمات `words.json`،
   و`source_room_tone()` بيرفض أي طبقة مش تحت الكلام بـ10 dB على الأقل أو أعلى من أرضية
   التسجيل +3 dB. **النتيجة على هذا المصدر: مرفوض أصلًا** (9.8 dB بس تحت الكلام، و%63 من
   «الوقفات» داخل نوى كلمات) — مفيش هواء نضيف مسروق من هنا؛ العلاج الحقيقي هو كثافة نص
   الجولة التالتة (وقفات ~0.16 ث مش ثوانٍ صفر رقمي)، مش طبقة ضجيج مُلَفّقة.
2. **`--air-source none` بقى هو المسار المنشور**: ملاءمة النطاق/الميل بس. النسخة دي عدّت
   **كل البوابات العشرة** ومنها الجديدة: `max_abs_difference = 0.0` و`rms_difference = −240
   dBFS` و`max_time_lag = 0 ms` مقابل `chain(dry)` المعلن — أي «مفيش أي حاجة اتضافت» مثبّت
   بالعيّنة، مش استنتاج. تقريرها `timing-repair/matched-render-verification.json` وملخص
   `matched-render-note.json`. المسلَّم المنشور لسه `source-sync/final-dub-source-sync.mp4`.
3. **أداء**: `np.correlate` على 30 ث كان بيخلي الفحص 20 دقيقة؛ FFT على 4 ث → ملف الاختبارات
   كله 0.3 ث. و`chain_air_evidence` بيرجع `bool` حقيقي وقيم Python scalar عشان JSON المنشور
   ما يقعش في `np.bool_`/NaN.
4. **توكن GitHub باظ في منتصف الجلسة** (`gh api /user` → 401، `git push` → Password
   authentication is not supported). **الجلسة مش مقفولة** — المطلوب إعادة ربط GitHub في Arena.
   للفترة دي: الكوميتات محفوظة محليًا ومنسوخة كـ`git format-patch` في `pending-pushes/`
   (الملفات دي بتنجى من إعادة ضبط الرنج بوكس)، وفيه `git fetch origin <sha>` للـsha المنشور.
   أول ما الربط يرجع: `git push origin arena/01a08487-dub` ثم `gh api …?ref=` للتأكيد قبل
   ما تُعرض أي لينك، وبعدها امسح `pending-pushes/`.
### 2026-09-09 (ليلًا متأخر) — العجز الحقيقي في النص أقل مما كان مقدَّرًا

`scripts/build_round3_brief.py` ( جديد، مع `round3-brief.md/json`) قارن النص المنشور
بميزانية المقاطع وجهًا-لوجه: **1457 كلمة منشورة مقابل 1633 مسموحة** فوق 630.6 ث =
عجز **176 كلمة (≈11%)** موزّع p0 +10، p1 +35، p2 +50، p3 … — لا 300+ كما كان التقدير
السابق («≈1270 كلمة تدخل» كان حسابًا على سرعة 2.0 ك/ث لصوت غير مختار).
**تصحيح مطلوب**: محاولة `script-dense.json` السابقة لم تكن «أرقّ بنصف النص» كما أوحت
قراءة خاطئة مني — الربط بالـclauses كان مختلًا (641 كلمة لـ123 مقطعًا)، والأداة الجديدة
تربط على مستوى النافذة بالـ8 takes المنشورة. القاعدة المستخلصة: عدد clauses لا يساوي
عدد segments أبدًا في `script-dense.json`، فالمقارنة يجب أن تكون نافذة-بنافذة.

**فحص المخرج** (`scripts/verify_source_sync_render.py`) صار يقيس من الأثر لا من الخطة:
`quiet_holes` بصمت رقمي مُقاس من العيّنات (ذروة < -55 dBFS على 10 مللي‑ثانية) متقاطعاً مع
كلام الأصل، و`clause_phase` يعيد قراءة بداية كل قطعة من `written_begin` ويقارنها بالوقت
المخطط وبمرساها، مع بوابتَي `no_quiet_hole_over_budget` و`clause_onsets_at_planned_time`.
تحذير: لا تُقِس مواضع الجُمل بـ VAD على المكساج — السرير الموسيقي يطمس البدايات (كانت 38
صفحة «بلا بداية» كاذبة)؛ قِس على الماستر الجاف، وللفجوات استخدم العيّنات لا الكاشف.
