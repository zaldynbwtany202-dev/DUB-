# ProStudio — دليل نظام الدبلجة الكامل

## تعريف النسخة

- الإصدار المحمول: `2026.09.07-latest`
- الالتزام المصدر: `b2d2f7f350f13d6da85d9b056ad9a8f764801d3f`
- الفرع المحمول: `main`
- واجهة الإنتاج: `docs/index.html`
- Workflow الرئيسي: `.github/workflows/dub.yml`

هذه الحزمة لقطة كاملة من النظام العامل، مع الكود والواجهة والاختبارات والمشاريع والفيديوهات والنتائج الموجودة. لا تحتوي Git history ولا قيم GitHub Secrets.

## سياسة محركات الصوت

المسار الافتراضي الوحيد هو:

```text
مرجع صوت أصلي أو عينة مخصصة
→ VoxCPM
→ Seed-VC عند توفره
→ ضبط التوقيت
→ فحص الكلمات
```

المحرك البديل الوحيد هو XTTS v2، وهو مقفل بطبقتين:

1. `allow_xtts=false` افتراضياً في GitHub Actions.
2. المحرك نفسه يرفض XTTS دون `--allow-xtts`.

Edge-TTS وQwen غير موجودين في خط الإنتاج أو واجهة الإنتاج. إعادة المحاولة لا تغيّر المحرك تلقائياً.

## مصادر نبرة الصوت

لكل متحدث خياران فقط:

- `source`: استخراج مرجع نظيف من كلام المتحدث في الفيديو.
- `custom`: ملف صوتي مخصص، ويفضل 6–20 ثانية من كلام نقي دون موسيقى.

يحفظ الملف في `library/<project>/voices/` وتصفه خريطة `voices.json`.

## سير العمل

```text
رفع الفيديو
→ FFmpeg: استخراج الصوت
→ Demucs: فصل الحوار عن الخلفية (اختياري)
→ Whisper: التفريغ والتوقيت واللغة
→ Pyannote: تحديد المتحدثين (اختياري)
→ تقسيم ذكي عند الجمل والوقفات
→ حفظ checkpoint
→ ترجمة واعية بالمدة، أو passthrough عندما المصدر=الهدف
→ VoxCPM الافتراضي أو XTTS المصرح به
→ Seed-VC لتحويل الهوية
→ مزامنة المدة دون قص الكلمات
→ Whisper: فحص الكلمات المنطوقة
→ إعادة المقطع الفاشل فقط بالمحرك نفسه
→ مزج الخلفية
→ تركيب الصوت على الفيديو
→ فحص اللغة والمدة والتغطية والصمت والتوافق
→ النشر في dubs/<project>/
```

## العربية إلى العربية

عندما تكون `source_lang=ar` و`target_lang=ar`:

- لا يُستدعى Gemini.
- لا تُعاد صياغة النص ترجمةً.
- ينتقل نص Whisper مباشرة إلى التوليد عبر `same_language_passthrough`.
- يمكن تغيير النبرة بعينة مخصصة مع VoxCPM وSeed-VC.

## نقاط الاستئناف

لكل مشروع draft release باسم `checkpoint-<project-id>`. يحفظ:

- تحليل الصوت والتفريغ.
- النص والترجمة.
- ملفات TTS.
- خرج Seed-VC.
- التوقيت وفحص الكلمات.
- المقاطع المكتملة.

عند إعادة التشغيل، المقاطع السليمة تُفحص ثم تُتجاوز. لا تُعاد إلا المراحل الفاشلة أو التي تغيّر مدخلها.

## الكاش والسرعة

- `.venv` تُستعاد قبل التثبيت.
- إذا لم توجد، تُبنى مرة واحدة وتُحفظ فوراً، حتى لو فشل الفيديو لاحقاً.
- أوزان Whisper محفوظة حسب حجم النموذج.
- XTTS لا يُحمّل إلا بعد التصريح.
- طول المقطع الافتراضي المستهدف 10 ثوانٍ لتقليل عدد طلبات TTS.

## قاطع ازدحام VoxCPM

الـSpace العام قد يعيد `Queue is full`. السياسة الحالية:

```text
محاولة واحدة
→ إذا ازدحم: حفظ checkpoint وإيقاف المرور فوراً
→ انتظار 15 ثانية
→ محاولة مرور أخيرة
→ نجاح أو توقف قابل للاستئناف
```

هذا يمنع عشرات الطلبات، لكنه لا يضيف GPU للخدمة العامة. للإنتاج الثابت استخدم Space خاصاً أو endpoint دائماً.

## تبديل خادم VoxCPM

من Settings → Secrets and variables → Actions → Variables:

- `VOXCPM_BACKEND=space`
- `VOXCPM_SPACE=<owner>/<private-or-dedicated-space>`
- `VOXCPM_MODEL=openbmb/VoxCPM2`

إذا لم تُضبط، يستخدم النظام `openbmb/VoxCPM-Demo` العام.

## التثبيت في مستودع جديد

1. أنشئ مستودعاً عاماً فارغاً باسم `prostudio`.
2. فك ZIP وارفع محتوياته إلى جذر فرع `main`؛ لا ترفع ZIP كملف واحد.
3. من Settings → Actions → General اختر **Read and write permissions**.
4. من Settings → Pages اختر **GitHub Actions**.
5. شغّل `Deploy ProStudio`.
6. افتح `https://اسم-الحساب.github.io/prostudio/`.

الواجهة تكتشف اسم الحساب والمستودع تلقائياً.

## GitHub Secrets المطلوبة

أضف القيم يدوياً؛ لا تضعها في commit:

- `HF_TOKEN`: الوصول إلى Hugging Face والموديلات.
- `TRANSLATE_API_KEY`: ترجمة اللغات المختلفة.
- `PYANNOTE`: عند استخدام تحديد المتحدثين إذا كان الإعداد يعتمد عليه.
- `YT_COOKIES`: اختياري، ويُستخدم فقط مع رابط YouTube.

## Variables الحالية للترجمة

- `TRANSLATE_PROVIDER=gemini`
- `TRANSLATE_MODEL=gemini-3.6-flash`
- `TRANSLATE_FALLBACK=fail`

`fail` يعني إيقافاً قابلاً للاستئناف عند فشل الترجمة، بدلاً من خلط محركات ترجمة مختلفة.

## بنية المجلدات

```text
.github/workflows/    تشغيل الدبلجة والنشر والفحوص
configs/              ملفات الإعدادات والملفات الشخصية
scripts/              محرك الدبلجة والاستئناف والنشر
python/youtube_auto_dub/ أو youtube_auto_dub/  الوحدات الأساسية
library/<project>/    المصدر والعينات وvoices.json
checkpoints/          حالة العمل المحلية أثناء الـrunner
dubs/<project>/       الفيديوهات النهائية وتقاريرها
docs/                 واجهة GitHub Pages
studio/ و web/         واجهات قديمة محفوظة للمرجع
voices/               بنك العينات المشتركة
tests/                اختبارات Python وواجهة المتصفح
```

## ملفات الناتج

عند النجاح ينشئ النظام:

- `final-dub-<run-id>.mp4`
- تقرير الجودة `.quality.json`
- تقرير المقاطع `.segments.json`
- تقرير اللغة `.language.json`
- `meta.json` لتاريخ إصدارات المشروع.

## فحوص الجودة

تشمل:

- تطابق مدة المصدر والناتج.
- عدم clipping.
- ترميز صوت متوافق AAC.
- صحة اللغة المستهدفة.
- تغطية المقاطع.
- عدم وجود فجوات طويلة.
- عدم فقدان كلام من التفريغ.
- ثقة ASR.

لا يُنشر الفيديو إذا فشلت البوابة النهائية.

## تشغيل مشروع من الواجهة

1. افتح GitHub Pages.
2. أدخل Fine-grained PAT للمستودع فقط.
3. ارفع الفيديو أو اختر مشروعاً من المكتبة.
4. اختر لغة المصدر والهدف.
5. لكل متحدث اختر `من الفيديو` أو `عينة مخصصة`.
6. اترك المسار الافتراضي VoxCPM ما لم تطلب XTTS صراحة.
7. شغّل الدبلجة وراقب المقاطع من صفحة التشغيلات.

## القيود المعروفة

- GitHub-hosted runner لا يملك GPU افتراضياً.
- VoxCPM المحلي 2B على CPU بطيء؛ الـSpace العام أسرع عندما يكون متاحاً لكنه غير مضمون.
- لا يمكن نسخ قيم GitHub Secrets من المستودع القديم.
- XTTS يحتاج تثبيتاً وأوزاناً إضافية ولا يعمل دون تصريح.
- دقة الناتج تعتمد على جودة العينة والتفريغ وصفاء الحوار.

## فحص سريع بعد النقل

1. شغّل workflow نشر Pages.
2. تحقق أن الواجهة تفتح من الحساب الجديد.
3. شغّل فيديو قصيراً بمرجع `source` وVoxCPM.
4. تأكد أن checkpoint draft release ظهر.
5. تحقق أن الفيديو النهائي موجود في `dubs/` وتقارير الجودة كلها ناجحة.
