# تفريغ الفيديو — مسودة للمراجعة قبل التسجيل

**الفيديو:** `source.mp4` — 7.6 MiB، نحو دقيقة و49 ثانية.

**تم تشغيل النموذج فعلياً:** Whisper Small متعدد اللغات، عبر whisper.cpp v1.7.6، على CPU محلي. اكتُشفت اللغة العربية باحتمال 99.583%؛ هذا **ليس** مقياساً لدقة الكلمات.

## الملفات

- `source.txt`: كلام الفيديو كما تعرّف عليه النموذج، دون تصحيح يدوي صامت.
- `source.srt`: 34 مقطعاً زمنياً.
- `source.segments.json`: 308 كلمات مع تقديرات التوقيت، واحتمالات الكلمات ونقاط DTW وبصمة الفيديو والنموذج.

## قبل توليد الصوت

- راجع النص إملائياً ولفظياً. توجد صيغ مشكوك فيها، مثل «دافلة» و«تتجاول»، لم نستبدلها تخميناً في النسخة الخام.
- راجع خصوصاً المقطع **20.560–34.280 ثانية** وما يليه حتى **39.800 ثانية**؛ طول النافذة وتوزيع الكلام يحتاجان تحققاً قبل المزامنة.
- يضم JSON تحذيرات `low_confidence_words` عند وجود كلمة ذات احتمال منخفض، و`long_segment_check_timing` عند تجاوز المقطع ثماني ثوانٍ. هذه مؤشرات مراجعة وليست حكماً قطعياً بخطأ الكلام.
- توقيتات الكلمات تقديرات من النموذج. نقاط DTW محفوظة منفصلة عن حدود الكلمات، ولا تمثل مزامنة شفاه مكتملة.

لم يُولّد أي صوت ولم تُنتج نسخة فيديو مدبلجة في مرحلة التفريغ هذه. بعد المراجعة، يُصاغ النص بالمصري ثم يولّد الوكيل الصوت المختار وتُجرى المزامنة في المستودع.
